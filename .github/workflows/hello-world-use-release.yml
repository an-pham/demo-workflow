name: Test
on: [workflow_dispatch]

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        uses: an-pham/hello-world-docker-action@v1
        with:
          who-to-greet: '@thisisanpham'
